public class merger {
    public static void main(String[] args) {
        int[] arr = {3,1,5,9,2,7};
        int low = 0, high = arr.length, mid = (low+high)/2;
        int[] x = merge(arr,low,mid,high);
    }

    private static int[] merge(int[] arr, int low, int high, int mid) {
        
        merge(arr)
        return arr;
    }
}
